import { CSS } from '../../lib/styled'

export const iconButtonBase: CSS = {
  justifyContent: 'center',
  alignItems: 'center',
  width: '1.5rem',
  height: '1.5rem',
  backgroundColor: '$muted',
  color: 'white',
  borderRadius: '$full'
}
